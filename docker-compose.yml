services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
      - .env.local
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
